properties:
  message:
    type: string
    description: The message sent to the user
  redirect:
    type: string
    maxLength: 2083
    minLength: 1
    format: uri
    description: >-
      The URL to be used to redirect the user after successful verification
type: object
required:
  - message
  - redirect
title: VerifiedOtpResponse
